# core-runtime

Shared runtime helpers for the Expo-first app shell.

This package maps already-exported runtime fixture bundles into app view models. It does not connect to Supabase, subscriptions, notifications, or the external content production pipeline.
