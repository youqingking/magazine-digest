# Stage REL1 Copy Audit

## Findings

- `feed` 仍直接显示 `Runtime ... adapter`。
- `search` 仍存在 `Follow`、`follows route` 等工程解释。
- `detail` 存在 `Audience`、`Access`、`Quota`、`Commercial` 等直接英文。
- `paywall` 存在 `Campaign`、`preview-only`、`provider`、`promo preview`。
- `profile` 存在 `Free Reader`、`Stage G`、`runtime`、`VIP days`、`saved_for_later`。
- `settings` 存在 `Runtime`、`Reader`、`Foundation`、`Build Audit`、`Runtime Proof`、`Quick 30s`、`Hybrid`、`Remote seam` 等。

## Scope

- 本轮仅清理普通用户表面与 settings 主区块。
- dev-only 诊断保留，但移动到低强调区块并改为说明性中文。
