# UI3.5 Runtime Closeout

- `screenshots/`: human runtime screenshots and compile/runtime captures
- `notes/`: operator notes and small text attachments
- `templates/`: tracked canonical templates for RC1 closeout

Real runtime evidence may stay repo-local and uncommitted when large. Finalize only trusts files that actually exist on disk.
