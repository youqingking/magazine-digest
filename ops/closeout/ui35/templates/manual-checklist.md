# RC1 Manual Checklist Template

Regenerate the working checklist with:

```powershell
node scripts/ops/prepare-runtime-closeout.mjs
```

Then fill:

- `output/stage-rc1/runtime-closeout-template.json`
- screenshot paths under `ops/closeout/ui35/screenshots/`
- optional notes under `ops/closeout/ui35/notes/`
